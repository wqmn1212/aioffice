# 수익화 검토

작성: 2026-09-16 · 대상 커밋 시점의 코드 기준

## 결론

**지금 상태로는 과금할 수 없습니다.** 기능이 부족해서가 아니라, 파는 물건이 아직 없기 때문입니다.

근거 세 가지를 코드에서 확인했습니다.

| 확인 항목 | 최초 검토 | 현재 |
|---|---|---|
| `tasks.output` | 항상 `null` | **해결** — BYOK 실행기가 채움 |
| 토큰·비용 기록 | 없음 | **해결** — 실행마다 input/output 토큰 저장 |
| `user_id` 컬럼 | 존재하지 않음 | **미해결** — 누구나 모든 회사를 조회 가능 |

> 2026-09-16 갱신: 모델 연동(BYOK)과 업무 실행기가 들어가면서 1번·3번 공백이
> 닫혔습니다. **남은 차단 요인은 멀티테넌시 하나**입니다.

`founding.ts`는 업무를 만들 때 output 자리에 `null`을 넣고, 그 뒤로 이 컬럼을 채우는 코드는 어디에도 없습니다. tick은 상태만 `대기 → 진행 → 검토 → 완료`로 넘깁니다. 화면은 살아 움직이지만 **결과물이 없습니다.**

돈을 받는 순간 이건 문제가 아니라 사고가 됩니다. 순서를 지켜야 합니다.

---

## 치명적 공백 (우선순위 순)

### 1. 산출물이 없다 — 유일한 진짜 구멍

나머지는 전부 배관 공사지만 이건 제품 그 자체입니다. 상세페이지 분석 담당이 실제로 이미지를 읽고 구간별 이탈 지점을 리포트로 내놓아야 합니다.

배관은 이미 깔려 있습니다:

- `tasks.output` 컬럼
- 역할별 도구 권한 (`RESEARCH` / `AUTHOR` / `BUILD`)
- 모델 등급 분리 (팀장 Opus, 실무 Sonnet, 조사 Haiku)

필요한 건 tick에서 Managed Agents를 호출해 `output`을 채우는 실행기 하나입니다.

### 2. 멀티테넌시가 없다 — 유료화 직전 필수

`companies` 테이블에 소유자가 없습니다. `GET /api/companies`는 **전체 회사 목록**을 돌려줍니다. 지금은 로컬 1인용이라 무해하지만, 배포하는 순간 남의 사업 설명과 업무 내역이 그대로 노출됩니다.

과금보다 먼저 해결해야 합니다. 결제 없는 유출은 사과로 끝나지만, 결제 후 유출은 환불과 분쟁입니다.

### 3. 사용량·원가 추적이 없다

Anthropic SDK 응답의 `usage`를 기록하지 않습니다. 업무 1건이 얼마인지 모르면 가격은 추측입니다. 이 서비스는 **정액 요금인데 변동 원가**를 지므로, 측정 없이 가격을 정하면 헤비 유저 몇 명이 마진을 전부 먹습니다.

### 4. 가치 도달 시간이 길다

회사를 세워도 "그래서 내가 뭘 얻었나"가 없습니다. 첫 세션에서 손에 잡히는 결과물 하나가 나와야 결제 전환이 생깁니다.

---

## 수익 모델

### 원가 구조가 가격 구조를 결정한다

```
업무 1건 원가 = (팀장 검토 토큰 × Opus 단가) + (실무 실행 토큰 × Sonnet 단가)
월 원가 = 업무 실행 건수 × 건당 원가
```

원가가 **인원수가 아니라 실행 건수**에 붙습니다. 그래서 "에이전트 30명"이 아니라 **"월 200건 실행"**으로 끊어야 합니다. 에이전트를 무제한으로 주고 실행량으로 과금하는 편이 원가와 정확히 맞습니다.

### 제안 요금제

| 플랜 | 가격 | 포함 | 상태 |
|---|---|---|---|
| 무료 | ₩0 | 데모 회사 1개, 시뮬레이션만 | 지금 가능 |
| 셀러 | ₩39,000/월 | 커머스·마케팅·전략 3부서, 월 200건 | 출시 예정 |
| 브랜드 | ₩129,000/월 | 전 부서, 월 800건, 데이터 연동 | 출시 예정 |

**가격은 확정이 아닙니다.** 건당 원가를 측정하기 전까지는 근거 없는 숫자입니다. 목표 마진을 70%로 잡으면 셀러 플랜의 허용 원가는 월 11,700원이고, 200건이면 건당 58원입니다. 이 숫자가 실측과 맞는지가 가격 검증의 전부입니다.

### 포지셔닝: 온라인셀러로 좁힐 것

"모든 업종의 AI 회사"는 팔기 어렵습니다. 경쟁자가 ChatGPT이고, 차별점을 설명하는 데 한 문단이 필요합니다.

반면 **"쿠팡·스마트스토어 셀러를 위한 AI 팀"**은 한 줄로 끝납니다. 커머스 부서가 이미 있고, 대상 고객이 명확하고, 성과 측정도 쉽습니다(전환율·광고 효율). 넓히는 건 나중에 해도 됩니다.

---

## 실행 순서

```
[1] 업무 실행기      ✅ 완료 — 회사가 연동한 제공자로 호출해 tasks.output 채움
[2] 사용량 기록      ✅ 완료 — 실행마다 provider/model/토큰 저장

[3] 계정·테넌시      user_id 추가, 모든 쿼리에 소유자 조건
     └ 남은 유일한 차단 요인. 배포 전 필수

[4] 결제            구독 + 실행량 한도

[5] 채널 지식        쿠팡/네이버 규칙을 부서 프롬프트에 주입
```

### BYOK가 요금 설계를 바꾼다

팀이 자기 키를 쓰면 **모델 비용을 우리가 부담하지 않습니다.** 원가 구조가 달라집니다.

| | 우리 키 | BYOK |
|---|---|---|
| 모델 비용 | 우리 부담 | 고객 부담 |
| 실행량 제한 이유 | 원가 방어 | 없음(서버 부하만) |
| 적정 과금 | 실행 건수 | 좌석·기능 |

BYOK는 **도입 장벽을 낮추고 마진을 안정시킵니다.** 이미 GPT·Claude를 결제 중인 팀에게는 "쓰던 키 그대로"가 강력한 설득 논리이고, 우리는 변동 원가에서 벗어납니다.

그래서 요금제를 이렇게 나누는 편이 낫습니다.

- **BYOK 플랜**: 자기 키 연동, 실행 무제한, 낮은 정액 — 초기 확산용
- **포함 플랜**: 우리 키 제공, 실행량 상한 — 키가 없는 팀용(원가 실측 후 가격 확정)

현재 코드는 BYOK만 지원합니다. 포함 플랜은 서버 소유 키를 fallback으로 쓰는 경로를 추가해야 하는데, 그 전에 반드시 실행량 상한이 있어야 합니다.

---

## 지금 할 수 있는 것: 수요 검증

제품이 완성되기 전에 확인할 수 있는 게 있습니다.

랜딩 페이지의 사전 신청 폼이 **판매 채널과 관심 플랜**을 함께 받습니다. 이건 허영 지표가 아니라 개발 순서를 정하는 데이터입니다.

- 쿠팡이 압도적이면 → 쿠팡 규칙부터 주입
- 자사몰이 많으면 → 카페24 연동을 먼저
- "아직 모르겠음"이 많으면 → 가격이 아니라 가치 설명이 부족한 것

```sql
SELECT channels, COUNT(*) FROM waitlist GROUP BY channels ORDER BY 2 DESC;
SELECT plan, COUNT(*) FROM waitlist GROUP BY plan;
SELECT note FROM waitlist WHERE note != '';
```

`note` 필드가 가장 중요합니다. "가장 맡기고 싶은 업무"에 적힌 내용이 곧 첫 번째로 만들어야 할 기능입니다.

---

## 정직하게 지켜야 할 선

랜딩 페이지에서 **무료 플랜에 "실제 산출물은 나오지 않습니다"를 명시**했고, 유료 플랜에는 "출시 예정" 태그를 붙였습니다.

시뮬레이션을 실제 작동하는 AI로 광고하면 단기 가입은 늘지만, 첫 사용에서 전부 이탈하고 환불 요구가 남습니다. 온라인셀러 시장은 좁아서 평판이 돕니다. 이 선은 유지하는 편이 이득입니다.
